# かいけいベース（reji）

アプリの表示名は「かいけいベース」です。ファイル名・フォルダ名・保存キーは `reji` のままにしています（既存データを引き継ぐため）。

## このプロジェクトは何か
特別支援学校の**販売学習・作業学習**で児童生徒が使うレジ（POS）アプリ。
スマホ・タブレット・パソコンのブラウザで動く。校内の製品販売や学校祭での会計に使う。

## 開発者について
- 非エンジニア（特別支援学校教員・ICT担当）。Web開発の基礎知識は多くない。
- 確立した開発パターン：**単一HTMLファイル + GitHub Pages**（必要に応じてSupabase）。
- ビルドツール・npm・フレームワークは使わない。ブラウザで開けば動く形を維持すること。

## 応答ルール（必ず守る）
- 日本語・です／ます調。結論ファースト、前置きなし。
- 専門用語にはカッコ書きで一言の補足（例：localStorage＝ブラウザ内にデータを保存する仕組み）。
- コードを提案するときは「なぜこの実装が良いか」を1〜2文添える。
- 不確実な点は推測せず「確認が必要な点」として明示する。
- ファイル全体をチャットに貼らない。編集はEditツールで差分適用する。
- 1回の編集は30行以内。大きな修正は複数回に分ける。
- 修正後は必ずJSの構文チェック（`node --check`）まで済ませてから報告する。

## 技術構成
- `index.html` … アプリ本体（HTML・CSS・JSすべてこの1ファイル）。**ファイル分割はしない。**
  GitHub Pages がURL直下で開くのは `index.html` なので、この名前から変えないこと。
- PWA用の付属ファイル（アプリ本体ではないので、ここは分けてよい）
  - `sw.js` … オフライン用のサービスワーカー
  - `manifest.webmanifest` … ホーム画面に追加したときの名前・アイコン
  - `icon-192.png` / `icon-512.png` / `apple-touch-icon.png` … アイコン
  - `logo-source.png` … ロゴの元画像（1024px）。アイコンを作り直すときの素材
- ロゴはヘッダーに **JPEGのbase64で直接埋め込み**（7KB）。外部ファイルにすると
  index.html を単体で開いたとき欠けるため。作り直す手順は下の「ロゴ」を参照。
- 外部ライブラリは**ファイル末尾に埋め込み済み**（CDNは使わない＝ネット接続不要）
  - JsBarcode 3.11.6 … バーコード画像の生成（CODE128形式／MIT）
  - html5-qrcode 2.3.8 … カメラでのバーコード読み取り（Apache-2.0）
- **ファイル構成に注意**：自分で書いたコードは 1000行あたりまで。それより下は
  ライブラリの圧縮コードで、1行がとても長い。**編集も読み取りもしないこと**
  （うっかり読むと大量の文字が流れる）。編集は必ず上半分だけを対象にする。
- データ保存：`localStorage`（キーは `reji.products` / `reji.sales`）。サーバーなし・端末内完結。

## 主要な機能と実装場所
| 機能 | 実装 |
|---|---|
| タブ切り替え | `#tabs` のクリックで `main > section` の `hidden` を切替 |
| 商品管理 | `products` 配列。`renderGoods()` `renderPList()` |
| カート | `cart` 配列。`addToCart()` `renderCart()` `cartTotal()` |
| 読み取り切替 | `setMode('camera'|'reader'|'off')`、読み取り結果は `hit(code)` に集約 |
| カメラ | `startCam()` / `stopCam()`（Html5Qrcode） |
| ハンディリーダー | `#scanIn` のEnterキーで確定。読み取り漏れ防止のため画面クリックでフォーカス復帰 |
| 会計 | `<dialog id="payDlg">`。`calcChange()` でおつり計算、`coinBreak()` で硬貨・紙幣の内訳 |
| バーコード発行 | `#mkLabel` でJsBarcodeを実行、`window.print()` で印刷 |
| 売上 | `sales` 配列。`renderSales()`、CSV書き出しは `#expS` |
| せってい | ヘッダーの歯車 → `<dialog id="setDlg">`。`reji.shop` に保存（`shop()` で読む） |
| 先生用ロック | `teacher` 変数と `LOCKED` 表。`askPin()` で `<dialog id="pinDlg">` を出す |
| バックアップ | `backupObj()` → `#bkExp` で書き出し、`#bkImp` で復元 |
| レシート | `receiptHTML()` で組み立て → `#receipts` に入れ、`printReceipts()` が印刷 |

## 設計上の決めごと（変更しないこと）
- **児童生徒が使う画面**なので、レジ画面のボタンは大きく、表示は「ひらがな中心」。
- おつりは大きな数字＋硬貨/紙幣の内訳を表示する（お釣りを数える学習に使うため）。これがこのアプリの核。
- 読み取り方法の既定は「リーダー」。カメラ常時起動は電池を消耗するため。
- 会計を取り消すと在庫も戻す。
- 商品の自作バーコードは `22` で始まる8桁（市販品のJANと衝突させないため）。

## デザイン（商品化を前提に一新／2026-07-31）
- 色は `:root` のCSS変数だけで管理する。個別の色指定を増やさない。
  - `--brand` 深緑＝アプリの基調　`--accent` 橙＝おつりだけに使う特別な色　`--danger` 赤＝消す操作
- 影は `--sh1`（カード）／`--sh2`（浮かせる）／`--sh3`（ダイアログ）の3段だけ使う。
- ヘッダーのロゴは元画像から切り出したJPEGをbase64で埋め込み（外部ファイルなし）。
- **お金は実物の形で見せる**：硬貨は丸、紙幣は長方形。会計の金額ボタン（`#moneyBtns`）と
  おつりの内訳（`#pCoins`）の両方でそろえる。ここがこのアプリの売りなので崩さないこと。
- おつりが足りないとき `.change` に `short` クラスが付き、赤に変わる。
- 「ぜんぶ けす」は `.ghost`（下線だけの控えめなボタン）。「お会計へ」の誤タップを防ぐため
  目立たせない。ここを目立つボタンに戻さないこと。
- 見た目の確認は `_preview-server.js`（開発専用。アプリ本体とは無関係、消しても動く）。
- ダークモードは**色変数の差し替えだけ**で成立させる。`[data-theme="dark"]` の打ち消しルールは
  「CSSに `#fff` などを直書きした箇所」の後始末なので、新しく白を直書きしないこと。
- 既定は端末の設定に追従。`#themeBtn` で切り替えると `reji.theme` に覚え、以後は手動優先。
- `<head>` の先頭にある小さなscriptは、画面が白く光るのを防ぐためのもの。消さないこと。
- 印刷（`@media print`）は画面がダークでも必ず白地・黒文字に戻す。

## 先生用ロック（さわるとき注意）
- `LOCKED` に入れたタブは児童生徒モードで開けない（今は しょうひん／バーコード／うりあげ）。
  レジ画面だけは常に開ける。**新しくタブを足したら `LOCKED` の見直しを忘れないこと。**
- 設定（歯車）と「うりあげを全部けす」も先生モード必須。**全部けすは先生モードでも
  あんしょう番号を再入力させる**（`askPin(..., 'check')`）。ここを素通しにしないこと。
- あんしょう番号は `pinHash()` で潰して `reji.pin` に保存。**これは誤操作を止める仕切りであって、
  本気の解析を防ぐ強度はない**。売り文句でも「セキュリティ」と書かないこと。
- 先生モードは画面を再読み込みすると解除される。加えて**5分さわらないと自動で戻る**
  （`bumpIdle()`）。先生がiPadを置いたまま離れる場面を想定している。
- 先生モードを抜けたとき、ロック中のタブを開いていたらレジ画面へ戻す。

## バックアップ（さわるとき注意）
- `backupObj()` が商品・売上・取消記録(`voids`)・設定・あんしょう番号・配色を1つのJSONにまとめる。
- `format` は互換性の判定に使う。**中身の構造を変えたら必ず `BK_VER` を上げること。**
  読み込み側は `format > BK_VER` のファイルを拒否する。
- `voids` は「売上を消さずに取消記録として残す」機能のための場所。まだ中身は空だが、
  先にバックアップ形式へ入れてあるので、実装しても古いバックアップと互換が保てる。
- 復元は全部入れかえ（追記ではない）。必ず件数と保存日を見せて確認を取ること。

## ロゴ
- 元画像は `logo-source.png`（1024×1024、クリーム地 `#FFF9E6`）。マーク部分だけの範囲は
  **L=321 / T=272 / 幅421 / 高さ349**。これより下は矢印の切れ端と文字が入るので含めないこと。
- ヘッダーは「マークの画像＋文字は本物のテキスト」。元画像の文字は「い」に矢印が
  重なって崩れているため、**画像の文字は使わない**。
- 作り直すときは元画像を上の範囲で切り抜き、クリーム地の正方形に収めて書き出す。
  ヘッダー用は128pxのJPEG（7KB）、アイコンは余白多め（マークが約7割）でPNG。

## レシート印刷のしくみ（さわるとき注意）
- 印刷は `body` に付ける一時的なクラスで切り替える。`rc`＝レシートを刷る、`a4`／`roll`＝紙の種類。
- 用紙サイズ（`@page`）は `<style id="pageCss">` へ**印刷の直前に書き込み、終わったら必ず空に戻す**。
  戻さないと、あとのバーコードラベル印刷まで58mm幅になってしまう。
- 後片付けは `afterprint` イベント＋3秒のタイマーの二段構え。iPadで `afterprint` が
  来ないことがあるため。`printReceipts()` は開始時に `a4`/`roll` を消してから付け直すこと。
- レシートは58mm幅（本文52mm）で作り、A4のときだけ3列グリッドに並べて切り取り線を付ける。
  **1つのHTMLで両対応**しているので、レイアウトを別々に作らないこと。
- 自動印刷は既定オフ。レシートプリンタのない学校が大半のため。

## 既知の制約・未対応
- カメラ読み取りは `https://` でないとブラウザが許可しない（GitHub Pages上で要実機確認）。
- 学校のiPadは管理設定でカメラが制限されている場合がある。
- **オフライン**：PWA化により、一度開けばURLからでも完全オフラインで動く（検証済み）。
  ただし**初回だけは通信が必要**。使う前に一度は開いておくこと。
- **更新は「次に開いたとき」に反映される**（表示を速くするため保存済みを先に出す方式）。
  更新があるときは画面に「あたらしい版があります」と出る。
- ファイルサイズは約476KB（うち約430KBがライブラリ）。初回表示が遅いと感じたら要検討。
- 複数端末の売上合算は未対応。
- 商品の編集が `prompt` を4回連続で出す方式。iPadでは操作しづらく、入力の検査もない。
- 会計を取り消すと売上記録そのものが消える（取消記録が残らない）。
- 正式販売に必要な規約・特商法表示・説明書・ライセンス全文がまだ無い。

## 次にやりたいこと（優先順）
1. **売上を消さず「取消記録」として残す** … `voids` に 取消日時・元の会計・操作者を積む
2. **商品編集を専用画面に** … いまは `prompt` を4回出す方式。値段・在庫・バーコードの検査がない
3. **規約・ライセンス・説明書** … 利用規約／プライバシーポリシー／特商法表示／操作説明書／
   第三者ライブラリのライセンス全文（JsBarcode:MIT、html5-qrcode:Apache-2.0）
4. 商品の並べ替え・カテゴリ分け（作業班ごとの表示）
5. **複数端末の売上合算** … Supabaseへの移行（既存パターンあり）

## 公開先
- リポジトリ `https://github.com/konkora780-ux/kaikei-base`（Public）
- 公開URL `https://konkora780-ux.github.io/kaikei-base/`
- 更新の手順は `git -C "D:\repos\reji" add -A` → `commit` → `push` の3手。
  Pagesが自動で反映する（1〜2分）。
- **`index.html` を変えたら `sw.js` の `CACHE` 名も上げること**（`kaikei-base-v1` → `-v2`）。
  上げ忘れると、端末に残った古いキャッシュが使われ続ける可能性がある。

## 実機で確かめること（まだ未確認）
- iPad から印刷したときのレシートの見え方（AirPrint／PDF保存）。
- 58mmロール紙の実機出力。レシートプリンタが手元にないため**画面上の計算でしか確認していない**。
- 学校のiPadでの初回表示の速さ（約460KB）。

## 済んだこと
- 2026-07-31 デザイン一新・ダークモード対応・アプリ名を「かいけいベース」に
- 2026-07-31 会計の「ひとつ もどす／ぜんぶ もどす」、カートの誤削除防止（✕ボタン方式）
- 2026-07-31 会計取り消しの照合を商品名→商品IDに変更、在庫0の品を取り消すと在庫が増えるバグを修正
- 2026-07-31 未登録バーコードでレジ画面から飛ばないように変更
- 2026-07-31 ライブラリをHTMLに埋め込み、CDNへの依存をなくした
- 2026-07-31 せってい画面（学校名・作業班名）と、レシート印刷（58mm／A4 両対応）を追加
- 2026-08-18 先生用ロック（あんしょう番号）と、まるごとバックアップ／復元を追加
- 2026-07-31 ロゴを差し替え（`logo-source.png` から生成）、PWA化、`reji.html` → `index.html` に改名

## 動作確認の手順
1. `index.html` をブラウザで開く（または公開URLを開く）
2. 「しょうひん」タブで商品を2〜3件登録（バーコードは空欄でよい）
3. 「レジ」タブで商品ボタンを押してカートに入り、合計が合うか確認
4. 「お会計へ」→ 金額ボタンで預り金を入れ、おつりと内訳が正しいか確認
5. 「バーコード」タブでラベルを作成し、印刷プレビューが崩れないか確認
6. 「うりあげ」タブで当日集計とCSV書き出しを確認
7. せんせいモードを抜け、ロックされたタブが開けないことを確認
8. せってい →「すべて バックアップ」→「すべて 復元」で元に戻ることを確認
